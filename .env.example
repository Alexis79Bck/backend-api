APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
DB_DATABASE=database/predictions.sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=backend_api
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# ============================================
# 🌍 APLICACIÓN
# ============================================
APP_NAME="Módulo API Rest | Sistema de Predicción de Animalitos"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

# Idioma y zona horaria
APP_LOCALE=es
APP_FALLBACK_LOCALE=en
APP_TIMEZONE=UTC

# ============================================
# 🗄️ BASE DE DATOS (SQLITE)
# ============================================
DB_CONNECTION=sqlite
DB_DATABASE=database/predictions_system.sqlite

# ============================================
# 🗄️ BASE DE DATOS (MYSQL)
# ============================================
# DB_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=predictions_system_db
# DB_USERNAME=root
# DB_PASSWORD=


# ============================================
# 📬 MAIL
# ============================================
MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="no-reply@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# ============================================
# 🔑 LARAVEL SANCTUM
# Configuración de autenticación API
# ============================================
SANCTUM_EXPIRATION=120  # Minutos de validez de un token (0 = no expira)
SANCTUM_STATEFUL_DOMAINS=localhost,127.0.0.1 # Dominios de confianza para SPA (separados por comas)
SANCTUM_COOKIE=laravel_sanctum # Prefijo para el nombre de las cookies de Sanctum


# ============================================
# 🛡️ LARAVEL FORTIFY
# Configuración de autenticación avanzada
# ============================================
FORTIFY_ENABLED=true  # Habilitar Fortify (true/false)
FORTIFY_SESSION_LIFETIME=120  # Tiempo de sesión en minutos (ej: 120 = 2h)
FORTIFY_EMAIL_VERIFICATION=true  # Habilitar verificación de correo (True/False)
FORTIFY_PASSWORD_RESET=true # Habilitar recuperación de contraseñas (True/False)
FORTIFY_TWO_FACTOR_AUTH=false  # Habilitar autenticación de dos factores (2FA) (True/False)

# ============================================
# 🎭 SPATIE LARAVEL-PERMISSION
# Configuración de roles y permisos
# ============================================
PERMISSION_DB_CONNECTION= # Conexión de BD (vacío = usa la global)
PERMISSION_TABLE_ROLES=roles  # Nombre de la tabla de roles
PERMISSION_TABLE_PERMISSIONS=permissions  # Nombre de la tabla de permisos
PERMISSION_TABLE_ROLE_HAS_USERS=model_has_roles  # Nombre de la tabla pivote para roles y usuarios
PERMISSION_TABLE_PERMISSION_HAS_USERS=model_has_permissions  # Nombre de la tabla pivote para permisos y usuarios
PERMISSION_CACHE_EXPIRATION=3600  # Tiempo de caché en segundos para roles/permissions

# ============================================
# 📜 SPATIE ACTIVITY LOGGER
# Configuración para registro de actividades
# ============================================
ACTIVITY_LOGGER_ENABLED=true  # Habilita o deshabilita el logger (true/false)
ACTIVITY_LOGGER_DB_CONNECTION= # Conexión de base de datos (vacío = conexión por defecto)
ACTIVITY_LOGGER_TABLE_NAME=activity_log # Nombre de la tabla de logs
ACTIVITY_LOGGER_DEFAULT_LOG_NAME=default # Nombre por defecto para el log si no se especifica otro
ACTIVITY_LOGGER_BATCH_SIZE=50 # Tamaño máximo de lote antes de persistir en DB
ACTIVITY_LOGGER_CLEANUP_DAYS=365 # Días que se conservarán los logs antes de limpieza automática
ACTIVITY_LOGGER_CLEANUP_FREQUENCY=1440 # Frecuencia de limpieza en minutos (ej: 1440 = cada 24h)

# ============================================
# 🛠️ DESARROLLO Y DEBUG
# ============================================
TELESCOPE_ENABLED=true
DEBUGBAR_ENABLED=true
